# Snyk (https://snyk.io) policy file, patches or ignores known vulnerabilities.
version: v1.25.1
# Production runtime (@sap/cds → express) is pinned via package.json overrides:
#   qs@6.16.0, body-parser@2.3.0, proxy-addr@2.0.8, ws@8.21.3
# @sap/cds-dk ships prebundled nested copies (lockfile resolved: undefined).
# postinstall scripts/patch-cds-dk-security-deps.mjs removes those nested
# vulnerable packages so Node resolves the overridden root versions.
# No active ignores required after that patch.
ignore: {}
patch: {}
